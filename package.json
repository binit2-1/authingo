{
  "name": "authingo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "pnpm --filter @authingo/react build",
    "release": "pnpm build && changeset publish",
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:it": "docker exec -it authingo-postgres psql -U postgres-healthcheck -d postgres-healthcheck"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.25.0",
  "private": "true",
  "devDependencies": {
    "@changesets/cli": "^2.30.0"
  }
}
